# system-monitor

## Description

Monitor system resources including CPU, memory, disk usage, and running processes. Supports both quick snapshots and continuous monitoring.

## Category

operations

## Script

monitor.sh

## Usage

```
./monitor.sh --mode snapshot
./monitor.sh --mode watch --interval 5
./monitor.sh --mode top --limit 10
./monitor.sh --mode gpu
```
