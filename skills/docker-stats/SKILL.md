# docker-stats

## Description

Monitor Docker containers — status, resource usage, logs, and health.

## Category

operations

## Script

stats.sh

## Usage

```
./stats.sh --mode status
./stats.sh --mode stats
./stats.sh --mode logs --container nginx
```
