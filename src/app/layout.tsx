import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Forge Studio — Dependency Tree",
  description: "Ultimate AI Lab Level Dependency Map. 50+ components, 106 connections, 9 layers, 11 database tables. From Ubuntu kernel to cloud users — every dependency mapped.",
  keywords: ["Forge Studio", "AI Lab", "Dependency Tree", "Architecture", "DevOps", "Infrastructure", "MCP", "LLM Proxy", "Self-Hosted"],
  authors: [{ name: "Jahanzaib Ali" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
